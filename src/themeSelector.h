#pragma once

#include <string>

bool
ReplaceContent(const std::string& men,
               const std::string& men2,
               const std::string& cafeBarista,
               const std::string& contentExtraPath);
